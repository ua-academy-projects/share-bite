version: '3.8'

services:
  gateway:
    image: nginx:alpine
    container_name: share-bite-gateway
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    restart: always