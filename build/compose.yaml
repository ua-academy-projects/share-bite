name: share-bite

include:
  - path: compose.infra.yaml
    env_file: ../.env

  - path: compose.s3.yaml
    env_file: ../.env

  # to run applications with build
  # - path: compose.apps.yaml
  #  env_file: ../.env

networks:
  share-bite-net:
    driver: bridge
