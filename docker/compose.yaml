name: share-bite

include:
  - path: compose.infra.yaml
    env_file: ../.env

  - path: compose.s3.yaml
    env_file: ../.env
    
  # to run applications with docker
  # - path: compose.apps.yaml
  #  env_file: ../.env

networks:
  share-bite-net:
    driver: bridge
